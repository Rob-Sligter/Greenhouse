# Requirements for web_watering.py (manual watering web interface)
# This service provides a web UI for manual plant watering

# Flask web framework (older stable version with pre-compiled wheels)
Flask==2.0.3

# Raspberry Pi GPIO for relay control
RPi.GPIO==0.7.1
