# Requirements for sensor_out.py (sensor collector service)
# This service reads sensors and sends data to InfluxDB

# Core dependencies (older stable versions)
influxdb==5.3.1
numpy==1.19.5
pytz==2021.3
astral==2.2
python-dateutil==2.8.2

# Raspberry Pi GPIO and sensor libraries
RPi.GPIO==0.7.1
dht11==0.1.0

# Adafruit libraries (older stable versions)
Adafruit-Blinka==6.21.0
adafruit-circuitpython-ads1x15==2.2.10
Adafruit-GPIO==1.0.3
