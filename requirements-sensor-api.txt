# Requirements for sensor_in.py (webhook API service)
# This service handles HTTP webhooks and controls relays

# Flask web framework (simplified)
Flask==2.2.2

# Raspberry Pi GPIO for relay control
RPi.GPIO==0.7.1

# HTTP dependencies for push notifications
requests==2.31.0
